export enum LoadingStates {
    'idle',
    'loading',
    'succeeded',
    'failed'
}

export enum TranslateCardType {
    Input,
    Output
}

export enum IconType {
    switch
}

export enum Color {
    LightBlue,
    DarkBlue,
    Midnight,
    DarkGrey,
    Grey,
    LightGrey,
    Blue,
    White,
    OffBlack,
    OffWhite,
    Red
}
