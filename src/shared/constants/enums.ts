export enum LoadingStates {
    'idle',
    'loading',
    'succeeded',
    'failed'
}

export enum TranslateCardType {
    Input,
    Output
}

export enum Color {
    LightBlue,
    DarkBlue,
    Midnight,
    DarkGrey,
    Grey,
    LightGrey,
    Blue,
    White,
    OffBlack,
    OffWhite,
    Red
}

export enum IconType {
    Undefined,
    Copy,
    Delete,
    Sound,
    Voice,
}

export enum Font {
    Primary,
    Light
}
