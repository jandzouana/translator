const Header = () =>{
    return(<nav id={"header"}>
        <div id={"title"} className={"disable-select"}>
            <span>Word</span>
            <span>Flex</span>
        </div>
    </nav>);
}

export default Header;
